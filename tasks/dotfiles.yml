- name: copy dotfiles
  copy:
    src: "../files/{{ item }}"
    dest: "~/{{ item }}"
  with_items:
    - ".aliases"
    - ".zshrc"
    - ".zlogin"
    - ".zlogout"
    - ".gitconfig"
    - ".gitignore.global"
    - ".vimrc"
    - ".tmux.conf"
